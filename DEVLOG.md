# DEVLOG

## [2026-08-27] 최종보스 아레나 스캐폴드 + 발판 통과 기능

### 사용 기술
- `Boss.cs`(`Enemy` 상속) — HP 75/50/25/10% 임계값에서 봄→여름→가을→겨울 순서로 계절 패턴 이벤트를 1회씩 발동시키고, 발동 중에는 `IsInvincible`로 데미지를 차단하는 구조. 기존 `Enemy`의 체력/피격/사망 로직을 그대로 재사용하고, override만 추가해서 부모 클래스는 건드리지 않았다.
- `BossHealthUI.cs` — DOTween `DOFillAmount`로 보스 체력바를 부드럽게 갱신. 기존 `PlayerHealthUI`는 하트 아이콘 방식이라 그대로 재사용할 수 없어서 fillAmount 기반으로 새로 작성.
- `OneWayPlatform.cs` — `Physics.IgnoreCollision` 기반으로 아래→위 점프 통과, 위→아래+점프 드롭스루를 구현. CharacterController도 Collider를 가지고 있어서 `Physics.IgnoreCollision`이 그대로 적용됐다.
- `PlayerController.cs`에 `OnDropThroughRequested` 이벤트 추가 — 새 입력을 만들지 않고 기존 Move 액션의 아래 방향 입력 + 점프 조합을 재사용했다.

### 어려웠던 점
- 사용자가 "아레나가 너무 좁아 보인다"고 피드백을 줬는데, 원인을 조사해보니 `SideViewCamera.target`이 애초에 비어 있어서 카메라가 플레이어를 전혀 추적하지 못하고 있던 실제 버그였다. offsetZ/FOV/offsetY 값을 조정해서 해결했다.
- 카메라 버그를 고치는 과정에서 더 심각한 문제를 발견했다: 플레이어 점프 물리(이동속도 5, 점프력 8, 중력 -20)로 도달 가능한 최대 점프 높이를 계산해보니, MidLeft/MidRight → TopCenter 플랫폼 간격이 5.0유닛이라 애초에 점프로 올라갈 수 없는 배치였다. 모든 플랫폼 간격을 검증된 안전치(2.4유닛)로 재배치하고 바닥 폭과 플랫폼 폭도 함께 넓혔다.
- 헤드리스(에디터 자동화) 환경에서는 도구 호출 사이에 실제 프레임/Time.time이 거의 흐르지 않아서, 발판 통과·드롭스루처럼 실시간 이동 거리와 조작감이 중요한 기능은 리플렉션만으로는 검증이 안 됐다. 이 부분은 사용자가 직접 Play Mode에서 플레이해보고 확인했다.

### 결과
- 4계절 패턴을 전부 사용하는 최종보스 "비둘킹"의 전용 아레나(`BossArena.unity`) 스캐폴드와, HP 임계값 기반 패턴 트리거 구조(`Boss.cs`), 체력바 UI, 발판 통과/드롭스루 기능이 완성되어 실제 플레이로 정상 작동을 확인했다. 다음 단계는 Phase 2(근접 공격 배선 + 원거리 투사체 신규 구현).

## [2026-08-29] 최종보스 근접/원거리 공격 구현 + 히트박스 회전 버그 수정

### 사용 기술
- `Boss.cs`에 `HandleBasicAttack()` 신설 — 사거리 안이면 기존 `EnemyCombat.StartAttack()`(근접), 밖이면 쿨다운 확인 후 원거리 투사체 발사로 분기. 계절 패턴(무적) 중에도 공격은 계속하고, 파훼 후 무방비 시간에는 공격을 멈추도록 만들어서 스펙에서 정한 "회피 압박 유지" 규칙을 지켰다.
- `BossProjectile.cs`(신규) — Rigidbody kinematic + `MovePosition` 방식 직선 투사체. 기존 `RainDrop`/`GinkgoFruit`와 동일한 이동 패턴을 재사용했다.
- Odin `[Button]`으로 원거리 공격 테스트 버튼 추가, 원거리 설정값(속도/데미지/쿨다운 등)은 Odin으로 정리.
- 헤드리스 검증에는 `EditorApplication.Step()`으로 프레임을 한 스텝씩 강제 진행시키는 방법을 새로 활용 — `Physics.Simulate()`와 달리 `Update()`/코루틴(공격 쿨다운 타이밍 등)까지 정상적으로 흐르게 할 수 있었다.

### 어려웠던 점
- 근접 공격이 실제로는 거의 맞지 않는 근본 버그를 발견했다: `EnemyMovement.FaceDirection()`이 좌우 반전을 Y축 90/-90도 회전으로 처리하는데, 여기 매달린 `AttackPoint`(로컬 X 오프셋)가 이 회전에 밀려 월드 좌표계에서 Z축으로 튀어나가 버렸다. 사이드뷰라 플레이어는 항상 Z=0 근처에 있는데 히트박스가 Z축으로 밀리니 사실상 안 맞는 상태였다. 이건 Boss뿐 아니라 SampleScene의 기존 `Enemy`에도 있던 잠재 버그였는데, 이번에 처음 발견해서 함께 고쳤다. 회전과 무관하게 항상 월드 X축에 고정되도록 기준 오프셋을 캐싱해두고 회전마다 역변환을 적용하는 방식으로 해결했다.
- 이 버그를 디버깅하는 과정에서 씬을 두 번 잘못 저장해 상태를 꼬이게 만들었다: Edit Mode에서는 `Awake()`가 자동 실행되지 않아 기준 오프셋 캐싱 없이 회전 로직을 테스트했다가 AttackPoint 위치가 진짜로 (0,0,0)으로 덮어써졌고, 이걸 복구한 뒤에도 회전이 적용된 상태 그대로 씬을 저장해버려서 버그가 다시 재발했다. 회전 보정 로직은 반드시 회전=항등, 오프셋=순수값인 상태에서만 저장해야 한다는 걸 알게 됐다.
- 원거리 투사체 데미지를 처음엔 8로 뒀는데, 사용자가 "플레이어 체력 5칸 중 8은 애매하다"고 피드백을 줘서 하트 1개(20)당 데미지로 맞춰 20으로 수정했다.

### 결과
- 보스가 사거리에 따라 근접/원거리 공격을 자동으로 골라 쓰고, 계절 패턴 중에는 무적 상태로도 공격을 계속하며, 파훼 후 무방비 시간에는 공격을 멈추는 기본 전투 패턴이 완성됐다. 근접 히트박스 회전 버그도 함께 수정해서 실제 플레이에서 정상적으로 타격이 들어가는 것을 확인했다.

## [2026-08-31] 최종보스 이동 AI (BossFlightMovement) — 발판 간 비행 이동

### 사용 기술
- `BossFlightMovement.cs`(신규) — 일정 주기(기본 5초) 타이머마다 `Boss.LandingPoints`(봄 패턴이 쓰던 착지 지점 목록을 `Boss.cs`로 승격시켜 공용 데이터로 재사용) 중 현재 위치를 제외한 랜덤 지점을 골라 `Rigidbody.MovePosition`으로 이동. 이동 중에는 봄 패턴 돌진과 동일한 물리 예외(`isKinematic=true`+`useGravity=false`)를 적용하고 도착 즉시 원복한다.
- `Boss.OnPatternTriggered`/`OnPatternEnded` 이벤트를 구독해서 계절 패턴 진행 중과 파훼 직후 무방비 시간 동안에는 이동 타이머를 정지시켰다. 기존 "HP 50% 이하부터만 좌우 추적" 로직은 완전히 폐기하고 HP 무관하게 처음부터 이동하도록 바꿨다.

### 어려웠던 점
- 비행 이동과 봄 패턴 돌진이 같은 `Rigidbody`를 각자 직접 제어하다 보니, 비행 도중 계절 패턴이 발동하는 엣지 케이스에서 물리 상태가 서로 겹칠 위험이 있었다. 계획에는 없었지만 비행 코루틴 내부에서 매 스텝 `_patternBlocking` 플래그를 확인해 즉시 중단하고 물리 상태를 복구하는 안전장치를 추가로 넣었다.
- 헤드리스 환경에서 `EditorApplication.Step()`으로 프레임을 강제 진행시켜 검증했는데, 실제 비행 속도감이나 타이밍이 자연스러운지는 리플렉션만으로 판단할 수 없어서 사용자가 직접 Play Mode로 확인했다.

### 결과
- 보스가 전투 시작부터 HP와 무관하게 5초 주기로 발판 사이를 랜덤하게 오가며 이동하게 됐고, 계절 패턴 발동 중/무방비 시간 중에는 이동이 정지되는 것까지 정상 작동을 확인했다.

## [2026-09-01] 최종보스 여름 패턴 (비/인력/레이저/대포 저지/그로기)

### 사용 기술
- `BossSummerPattern.cs`(신규) — 상승(호버링, `Boss.SetFlying(true)`로 공격 정지) → 비(`RainDrop` 재사용 스폰) → 인력(플레이어+대포를 `SetWindZone`/`ApplyPull`로 동시 견인) → 레이저(중앙 세로 밴드 지속 피해) 사이클을 관리. 대포알이 명중하면 레이저를 즉시 중단하고 보스를 자유낙하시켜(중력 복원만으로 처리, 별도 하강 로직 불필요) `Boss.NotifyPatternSolved()`를 호출한다.
- `BossCannon.cs`(신규) — `VendingMachine.cs`의 Interact 프롬프트 구조를 재사용해 왼쪽 끝까지만 견인되는 대포 상호작용을 구현.
- `BossCannonball.cs`/`BossLaserBeam.cs`(신규) — 각각 `BossProjectile.cs`의 직선 이동 패턴, 경고(빨강 깜빡임)→판정(파랑, 초당 데미지)의 2단계 레이저를 구현. 레이저는 Core/Glow 두 겹 Transparent 머티리얼로 두께감을 표현했다(이 프로젝트에서 처음으로 Transparent Surface Type 채택).
- "그로기" 상태와 "레이저 중단 후 추락"은 신규 시스템을 만들지 않고 기존 `Boss.NotifyPatternSolved()`가 만드는 무방비 시간과 중력 복원만으로 해결했다 — 호버링 위치가 정확히 착지 발판 위였던 덕분에 별도의 제어된 하강 로직이 필요 없었다.

### 어려웠던 점
- 대포가 발사되지 않는 버그가 있었다: 공유 `Interact` 입력 액션이 다른 오브젝트(`VendingMachine`)를 위해 Hold 인터랙션으로 설정돼 있어서 짧게 눌렀을 때 `performed`가 발생하지 않았다. 공유 액션 자체는 건드리지 않고 `BossCannon.cs`만 `started`(누르는 즉시) 이벤트로 구독하도록 바꿔서 해결했다.
- 레이저가 바닥에 닿지 않는 버그의 원인은 "보스 착지 좌표"(콜라이더 반높이만큼 떠 있음)와 "실제 지면 좌표"를 착각해서 재사용한 것이었다 — 이 프로젝트에서 두 번째로 같은 종류의 좌표 혼동이 발생했다.
- 인력 세기, 레이저 경고 시간, 레이저 지속시간을 실플레이 피드백에 맞춰 여러 차례 재조정했다(인력 4→9, 경고 0.3→1.5초, 레이저 지속 3.5→5초).
- execute_code로 스크립트에 필드를 새로 추가한 직후 바로 리플렉션/SerializedObject로 접근하면 도메인 리로드가 끝나기 전이라 구 버전 어셈블리를 참조해서 실패하는 일이 여러 번 있었다.

### 결과
- 여름 패턴(비→인력→레이저→대포 저지→그로기) 전체 사이클과 사운드/연출까지 완성돼서 실제 플레이로 정상 작동을 확인했다.

## [2026-09-03] 공중 상승점프(더블점프) + BossArena 발판 배치 재조정 + 카메라 Y축 추적 버그 수정

### 사용 기술
- `PlayerController.cs`에 `airJumpForce`(신규 Odin 필드) 추가 — 공중에서 위 방향키+점프 입력 시 착지 전까지 단 1회만 발동하는 상승점프. 기존 접지 점프/드롭스루 입력 분기(`OnJumpPerformed()`)를 건드리지 않고 새 분기만 추가했다. `PlayerController`는 전체 씬이 공유하는 공용 스크립트라, 이 기능이 BossArena뿐 아니라 SampleScene 등에도 영향을 준다는 점을 미리 사용자에게 고지하고 승인받은 뒤 진행했다.
- `SideViewCamera.cs`의 Y축 추적 로직을 고정값에서 `target.position.y + offsetY`(플레이어 기준 상대 오프셋)로 수정 — X축은 원래부터 상대 오프셋이었는데 Y축만 고정이었던 기존 설계의 한계였다.
- 발판/보스 관련 좌표(`Boss.landingPoints`, `BossSummerPattern.hoverPosition`/`rainSpawnY`, `BossCannon.stopX` 등) 재조정 시 Unity MCP로 씬의 실제 Transform 값을 매번 직접 조회해서 확인한 뒤 반영하는 방식을 반복했다(추측으로 값을 바꾸지 않음).

### 어려웠던 점
- 발판을 사용자 손그림 기준으로 훨씬 높게 띄우려니, 기존 점프력(최대 1.6유닛)으로는 바닥에서 곧장 닿을 수 없는 높이가 필요했다. 이 모순을 해결하기 위해 공중 상승점프(더블점프)를 신규로 추가하기로 방향을 정했다.
- 발판 사이 높이차를 손그림에서 처음 추정했을 때(약 0.6~0.7유닛)와 실제 게임 스크린샷을 플레이어 키(2.0유닛) 기준으로 다시 정밀 측정했을 때(약 3.0~3.6유닛)가 4배 넘게 차이났다. 손그림 비율만으로 수치를 한 번에 확정하지 않고, 실제 게임 스크린샷과 교차검증하는 과정이 필요했다.
- 발판을 6~7유닛 높이까지 올린 뒤에야 카메라가 플레이어를 못 따라가는 문제가 드러났는데, 원인은 `SideViewCamera.cs`가 애초부터 Y축을 고정값으로 처리하던 잠재 설계 결함이었다(발판이 낮았던 지금까지는 티가 안 났을 뿐, 이번에 새로 생긴 버그는 아니었다).
- 값 하나(예: 발판 Y좌표)를 바꾸면 그 값에 상대적으로 튜닝돼 있던 다른 상수(`hoverPosition`, `rainSpawnY` 등)도 같이 깨질 수 있어서, 관련 상수를 매번 grep으로 재확인하는 과정이 필요했다.

### 결과
- 공중 상승점프로 기존 점프력만으로는 닿지 않던 높은 발판까지 이동할 수 있게 됐고, 발판 배치가 손그림 구도(지그재그, 높은 고도차)에 맞게 재조정됐으며, 카메라가 높이 변화에도 플레이어를 정상적으로 따라가는 것을 실제 플레이로 확인했다.
